// A simple object to store our role names
const ROLES = {
  USER: 'user',   // The default role for everyone
  ADMIN: 'admin', // Future-proofing: we might need this later
  MENTOR: 'mentor' // Future-proofing: another potential role
};

module.exports = ROLES;